# TODO

## P0 - 数据安全

- [ ] SQLite 备份改为一致性快照，避免直接复制正在写入的数据库、WAL 和 SHM。
- [x] 部分恢复失败时保留备份，并向调用方返回失败及已恢复/未恢复文件信息。
- [ ] 删除会话前自动创建可恢复备份，并为跨 history、SQLite、会话文件的失败增加补偿机制。
- [ ] 为备份、恢复、删除和 JSONL 重写增加集成测试，包括中途失败场景。

## P1 - 功能正确性

- [x] 侧栏搜索使用全局内容搜索，准确显示命中数和涉及的会话数，并支持分页。
- [ ] 为搜索和会话切换请求增加取消或请求序号，防止旧响应覆盖新状态。
- [ ] 设计安全的批量替换流程：预览命中、限定字段、自动备份、逐项确认和失败回滚。
- [x] 历史分页与时间线一致，在浏览器中记住每页条数。
- [x] 备份面板增加加载、空状态和错误反馈。

## P2 - 性能与安全

- [ ] 将 JSONL、history 和目录扫描移出 Tokio 异步工作线程。
- [ ] 为大数据量搜索建立索引或 SQLite FTS，避免每次扫描所有会话文件。
- [ ] 非回环地址监听时要求访问令牌，并限制 CORS 和文件系统浏览接口。

## P3 - 工程质量

- [ ] CI 增加 `cargo test`、Clippy、前端单元测试和关键流程端到端测试。
- [ ] 修正文档中的 Rust、Node.js 最低版本和安装说明。
- [ ] 清理旧备份模态框及重复状态。
- [ ] 拆分前端产物，降低首屏 JavaScript 体积。
- [x] 修复 `cargo clippy --all-targets -- -D warnings` 的告警。
